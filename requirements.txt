# Runtime + dev dependencies, pinned to the versions this project was built and
# tested against. Pinning keeps the deployed Streamlit Cloud app reproducible —
# unpinned, a future pandas/numpy release could silently break the live app.
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
statsmodels==0.14.6
fredapi==0.5.2
streamlit==1.58.0
plotly==6.8.0
matplotlib==3.10.9
pyarrow==24.0.0
python-dotenv==1.2.2
pytest==9.0.3
